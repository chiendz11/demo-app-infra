global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Alloy owns target discovery and scraping. Prometheus only stores and queries metrics
# received through the remote-write receiver.
scrape_configs: []
